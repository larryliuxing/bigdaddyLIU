SESSION_SECRET=guild-dev-secret-change-me-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
# Set to true only when the site is served over HTTPS.
# Leave false (or omit) for plain HTTP IP access so login cookies work.
COOKIE_SECURE=false
